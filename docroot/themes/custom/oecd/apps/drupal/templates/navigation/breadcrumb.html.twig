{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}

{{ attach_library('oecd/breadcrumb') }}

{# Template #}
{% if breadcrumb %}
  {% include "@molecules/breadcrumb/breadcrumb.twig" with {
    variant: 'normal',
    items: breadcrumb
  } %}
{% endif %}